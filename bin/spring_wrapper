#!env ruby
if File.exist?('config/application.rb') || File.exist?('config/spring.rb')
  exec 'bundle', 'exec', 'spring', *ARGV
else
  if File.exist?('Gemfile')
    exec 'bundle', 'exec', *ARGV
  else
    exec *ARGV
  end
end
