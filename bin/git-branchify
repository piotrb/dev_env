#!/usr/bin/env ruby

if ARGV.empty?
  name = $stdin.readline
else
  name = ARGV.join(' ')
end

name = name.downcase.gsub(/[^a-z0-9]/, ' ').gsub(/ +/, ' ').strip.gsub(' ', '-')

system "git checkout -b #{name.inspect}"
